
const $=id=>document.getElementById(id);function localDate(d=new Date()){return new Intl.DateTimeFormat("en-CA",{timeZone:"Europe/Paris",year:"numeric",month:"2-digit",day:"2-digit"}).format(d)}function esc(v){return String(v??"").replaceAll("&","&amp;").replaceAll("<","&lt;")}function addDays(date,n){const d=new Date(date+"T12:00:00");d.setDate(d.getDate()+n);return localDate(d)}async function load(){try{$("message").textContent="Chargement…";const r=await fetch(`/api/planning/workshop?from=${$("from").value}&to=${$("to").value}`);const p=await r.json();if(!r.ok)throw new Error(p.error);const groups=new Map();for(const item of p.items){if(!groups.has(item.planning_date))groups.set(item.planning_date,[]);groups.get(item.planning_date).push(item)}$("days").innerHTML=groups.size?[...groups].map(([date,items])=>`<section class="day"><h2>${new Date(date+"T12:00:00").toLocaleDateString("fr-FR",{weekday:"long",day:"numeric",month:"long",year:"numeric"})}</h2><div class="items">${items.map(i=>`<article class="item"><h3>${esc(i.registration||i.entity_name||"Véhicule")}</h3><p><strong>${esc(i.activity_label||"Rendez-vous atelier")}</strong></p><p>${i.start_time||"—"} → ${i.end_time||"—"} · ${esc(i.location||"Lieu non précisé")}</p><p>${esc(i.details||"")}</p></article>`).join("")}</div></section>`).join(""):`<p>Aucun planning atelier importé sur cette période.</p>`;$("message").textContent=""}catch(e){$("message").textContent=e.message}}const today=localDate();$("from").value=today;$("to").value=addDays(today,7);$("refresh").onclick=load;load();
